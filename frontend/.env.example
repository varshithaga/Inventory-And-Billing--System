# Google OAuth client ID for "Sign in with Google" on the login page.
# Create one at https://console.cloud.google.com/apis/credentials
# (OAuth client type: "Web application"), add this app's dev/prod origins
# under "Authorized JavaScript origins", then copy the client ID here.
# Must match GOOGLE_OAUTH_CLIENT_ID in the backend's environment.
VITE_GOOGLE_CLIENT_ID=378850829063-ippubegvbdhhd9p3okkohs8d45l60km8.apps.googleusercontent.com
